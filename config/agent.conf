# Branch Agent 配置文件
# 部署路径: /etc/wg-monitor/agent.conf

[agent]
branch_id = branch-shanghai-01
secret = REPLACE_WITH_MATCHING_SECRET_FROM_SECRETS_JSON
hq_endpoint = http://10.10.0.1:8080/monitor/api/report
report_interval = 5
interfaces = eth0,eth1,wg0

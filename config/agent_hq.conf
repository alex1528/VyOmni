# VyOmni HQ Agent 配置
# 部署位置: /opt/vyomni-agent/config.conf

# 监控平台地址（Linux 服务器 IP）
server_url = http://192.168.1.100:9100

# 上报间隔（秒）
report_interval = 5

# HMAC 签名密钥（须与服务器 secrets.json 一致）
hmac_key = change-me-in-production

# 本机标识
hostname = HQ-VyOS
role = hq
